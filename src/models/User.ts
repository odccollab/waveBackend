import Model from '../core/Model';
 class User extends Model {
    constructor() {
        super();
    }

    // Ajoutez des méthodes spécifiques à `Post` si nécessaire
}
const UserModel = new User()
export default UserModel;